<div class="flex flex-col text-center justify-center items-center my-auto mx-8">
  <h1 class="text-3xl">My Addresses</h1>

  <div class="flex flex-col">
    {% for address in customer.addresses %}
      <div>
        <div class="">
          {{ address | format_address }}
        </div>
      </div>
    {% endfor %}
  </div>
</div>"
<style>
  .dropup {
    position: relative;
    display: inline-block;
  }

  .localeBtn {
    background-color: skyblue;
    color: white;
    padding: 6px;
    font-size: 20px;
    position: fixed;
    bottom: 25px;
    right: 25px;
    border-radius: 50%;
    max-width: 70px;
    max-height: 70px;
    box-shadow: -1px 0 20px 0 #00000066;
  }
</style>

{% form 'localization', id: "#localization_form_tag", class: "dropup" %}
  <div class="localeBtn">{{ 'globe.png' | asset_url: 'small' }}</div>
  <div class="dropup-content">
    {% for locale in shop.published_locales %}
      <a href="#">{{ locale.endonym_name }}</a>
    {% endfor %}
  </div>
{% endform %}